package org.example;

public class GameHandler {
	public GameHandler(byte[] board) {
		
	}
	
	public boolean makeMove(String move) {
		return isValidMove(move);
	}
	
	private boolean isValidMove(String move) {
		return true;
	}
}
